ZOOM_CLIENT_ID=
ZOOM_CLIENT_SECRET=
ZOOM_WEBHOOK_SECRET_TOKEN=
# Optional — not needed if users provide their own key via the sidebar
MOVERIS_API_KEY=
PORT=8080
LOG_LEVEL=info
# Set to false to require sidebar-initiated scans (no auto-start on webhook)
AUTO_START_RTMS=true
# Optional — auto-generated if not set
JWT_SECRET=
